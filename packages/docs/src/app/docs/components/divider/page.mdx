import { LivePlayground } from "@/components/LivePlayground";
import { PropsTable } from "@/components/PropsTable";

# Divider

A horizontal or vertical separator for visually dividing content.

## Example

<LivePlayground code={`
<div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
  <p style={{ margin: 0, fontSize: "0.875rem" }}>Content above</p>
  <Divider />
  <p style={{ margin: 0, fontSize: "0.875rem" }}>Content below</p>
  <div style={{ display: "flex", alignItems: "center", gap: 16, height: 40 }}>
    <span>Left</span>
    <Divider orientation="vertical" />
    <span>Right</span>
  </div>
</div>
`} />

## Props

<PropsTable data={[
  { name: "orientation", type: '"horizontal" | "vertical"', default: '"horizontal"', description: "Direction of the divider." },
  { name: "className", type: "string", default: "—", description: "Additional CSS class names." },
]} />

## Code

```tsx
import { Divider } from "@orbi/react";

<Divider />
<Divider orientation="vertical" />
```
